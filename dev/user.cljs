(ns cljs.user
  (:require [re-frisk.core :refer [enable-frisk! add-data]]))

;; (enable-frisk!)
;; (add-data :game (add-data :game codenames.core/game))
;; (add-data :view (add-data :view codenames.core/view))
